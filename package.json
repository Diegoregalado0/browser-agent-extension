{
  "name": "browser-agent",
  "version": "1.0.0",
  "private": true,
  "description": "A browser that runs your own AI browsing agent: a Chrome extension, bring your own API key.",
  "type": "module",
  "scripts": {
    "build": "node scripts/build-extension.js",
    "check": "for f in src/*.js src/providers/*.js ui/*.js extension/*.js scripts/*.js; do node --check $f || exit 1; done"
  },
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.128.0",
    "@google/genai": "^2.24.0",
    "ollama": "^0.6.3",
    "openai": "^7.22.0"
  },
  "devDependencies": {
    "esbuild": "^0.28.2"
  }
}
